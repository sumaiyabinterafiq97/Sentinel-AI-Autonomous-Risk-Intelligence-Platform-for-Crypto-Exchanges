{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sentinel-ai.internal/schemas/events/mvp/auth/UserLoggedIn.v1.schema.json",
  "title": "UserLoggedInPayload",
  "description": "Published by AUTH. Consumer: SEC (V2).",
  "type": "object",
  "required": ["userId", "sessionId", "loggedInAt"],
  "properties": {
    "userId": { "type": "string", "format": "uuid" },
    "sessionId": { "type": "string", "format": "uuid" },
    "organizationId": { "type": "string", "format": "uuid" },
    "loggedInAt": { "type": "string", "format": "date-time" },
    "deviceId": { "type": ["string", "null"], "format": "uuid" }
  },
  "additionalProperties": false
}
